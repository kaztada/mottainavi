# roadmap.md — 実装ロードマップ(Claude Code用フェーズ分割)

バージョン: v0.1 (2026-07-04)
実装期間: 2026-07-05 〜 07-07

---

## 進め方の原則

- 1フェーズ = Claude Code の1セッション目安。**プランモードで計画→承認→実装→動作確認→コミット**を各フェーズで回す
- 各フェーズの「完了条件」を満たしてから次へ。満たせない場合はスコープを削る(機能を削っても完了条件は守る)
- 詰まったら15分ルール: 15分解決しなければ回避策(モック・手動データ・機能削減)に切替

---

## Phase 0: プロジェクト初期化(7/5 午前・30分)

やること:
- Next.js 15 + TypeScript + Tailwind v4 でプロジェクト作成
- docs/(設計書一式)と CLAUDE.md を配置
- ESLint/Prettier、zod、fuse.js、cheerio、tsx、vitest 導入
- Vercel 連携(空のトップページで初回デプロイまで)

完了条件:
- [ ] `npm run dev` でHello World表示
- [ ] Vercel本番URLで表示確認
- [ ] 初回コミット+push

## Phase 1: データパイプライン(7/5 午前〜午後・3〜4h)★最重要

やること:
- scripts/build-data.ts 実装(data-model.md §3の fetch→parse→enrich→validate→emit)
- 大阪市一覧表HTMLのローカルキャッシュ機構
- categories.json / municipalities.json / aliases.json(初期版・主要50語) / reuse-options.json を作成
- reuse_category ヒューリスティック実装+reuse-overrides.json 機構
- パース結果の統計表示(品目数、区分別件数、reuse付与率、未解決区分)

完了条件:
- [ ] `npm run build-data` で osaka-city.json 生成(目安1,000品目以上)
- [ ] zodスキーマ検証パス
- [ ] 未解決の区分ラベルが0(全ラベルがcategory_idにマップされる)
- [ ] スポットチェック10品目(アイロン/ソファ/ペットボトル/ギター/エアコン/スプレー缶/衣類/絵本/一升びん/モバイルバッテリー相当品)が公式表と一致

注意:
- 市サイトへのアクセスは礼儀正しく(1回取得してキャッシュ。連打しない)
- 「収集しません」「家電リサイクル法対象」品目の注意文言を落とさないこと(誤案内リスクが最も高い箇所)

## Phase 2: 検索コア+基本UI(7/5 午後〜夜・3〜4h)

やること:
- lib/search.ts(正規化+Fuse.js、tech-stack.md §4)+ユニットテスト
- S1/S2実装: 検索ボックス、インクリメンタル結果、区分バッジ、0件時フォールバック
- S3実装(捨て方セクションのみ): dispositions表示、手数料、申込リンク、公式リンク、出典表記
- /item/[id] のSSG(generateStaticParams)

完了条件:
- [ ] 「ぎたー」「PET」「iron」でそれぞれ期待品目がヒット
- [ ] 検索→詳細が3タップ以内
- [ ] スマホ実機(iPhone)で入力〜表示のストレスなし
- [ ] 出典表記(CC-BY 4.0)が全詳細ページに出る

## Phase 3: 手放し導線(7/6 午前・2〜3h)★魂

やること:
- ReuseOptionCard 実装(effort/moneyタグ含む)
- S3で reuse_category がある品目に「捨てる前に、こんな手も」セクションを表示(screens.md §5の順序・トーン)
- S2リストに ♻️ インジケータ
- reuse-options.json の文言を #ちるエコ日和 トーンで磨く(Kazレビュー)

完了条件:
- [ ] ギター/子ども服/ソファ/絵本/一升びん で意図した手放し選択肢が表示される
- [ ] 手放しセクションが「押しつけがましくない」見た目になっている(Kaz主観チェック)
- [ ] reuse付与率レポート確認(粗大ごみ品目の付与率がおおむね5割超)

## Phase 4: 多言語(7/6 午後・2〜3h)

やること:
- lib/i18n.tsx(Provider+useT+LangToggle、localStorage保持)
- ui.ja.json / ui.en.json 全キー対訳
- items.en.json: 頻出300品目の英訳を Claude Code で下訳生成 → Kazレビュー → 反映
- 英語モードのS3表示ルール(screens.md §5)実装

完了条件:
- [ ] ENトグルでUI全文言が英語化
- [ ] "sofa" "battery" "guitar" で英語検索ヒット
- [ ] 英訳未整備品目でもレイアウト破綻しない

## Phase 5: 仕上げ・公開(7/7・3〜4h)

やること:
- S4(About)ページ、OGP画像、favicon
- デザイン最終調整(#ちるエコ日和トーン: 色・余白・角丸・コピー)
- Lighthouse計測と改善(モバイルPerformance/A11y 90+目標)
- Vercel Analytics有効化
- 実機テスト(家族・友人2〜3人に触ってもらう)
- README整備(スクショ、出典、ライセンス)
- 必要ならドメイン設定(例: kaztada.eco のサブドメイン)

完了条件:
- [ ] 本番URLで全機能動作
- [ ] Lighthouse モバイル 90+(Perf/A11y)
- [ ] 第三者1名以上が説明なしで「ソファの捨て方と手放し方」に到達できる
- [ ] 紹介用スクショ1枚と一言コピー(SNS発信用)ができている

---

## バッファ計画(遅れた場合の削減順)

1. Phase 4の品目英訳を300→100件に縮小(UI英語化は維持)
2. S2の「区分から見る」導線を削除(検索のみに)
3. OGP画像を文字だけの簡易版に
4. **削らないもの**: 出典表記、誤案内リスク対策(公式リンク常設)、手放し導線そのもの

## 7/7以降の育て方(メモ)

- 使用ログを見て別名辞書を拡充(検索0件クエリの回収)
- 品目英訳の段階的拡大
- 堺市など近隣自治体の追加検討
- #ちるエコ日和 コンテンツ化(制作過程の発信、ライブMCでの紹介)
